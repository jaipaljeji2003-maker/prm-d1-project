{
  "name": "prm-d1-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev":    "wrangler dev --port 8788",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute prm-dispatch --file=schema.sql",
    "db:init:local": "wrangler d1 execute prm-dispatch --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
